<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />

    <style name="calculatorButton">
        <item name="android:layout_width">76dp</item>
        <item name="android:layout_height">76dp</item>
        <item name="android:elevation">0dp</item>
        <item name="fontFamily">@font/doppio_one_regular</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:textSize">24sp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="calculatorButton.number" parent="calculatorButton">
        <item name="android:background">@drawable/circle_button</item>
        <item name="android:textColor">@color/numberColor</item>
    </style>

    <style name="calculatorButton.property" parent="calculatorButton">
        <item name="android:background">@drawable/circle_button_dark</item>
        <item name="android:textColor">@color/main</item>
    </style>
</resources>